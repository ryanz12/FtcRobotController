package org.firstinspires.ftc.teamcode.Auto;

public class BlueAutoFar {

    // (x, y, a) -> (x, -y, -a)
    // Convert all paths using this formula
    // Unless you can figure out how to actually use pose mapping
    // Even then, it would be good to create separate coordinates
}
